# Spring Cloud 2022.0.0 官方仅支持 Boot 3.0.x，项目已升级到 3.2.5，关闭兼容性校验以正常启动
spring:
  cloud:
    compatibility-verifier:
      enabled: false

ai:
  gateway:
    route-enabled: true
    uri: ${AI_GATEWAY_URI:lb://oj-agent}
    public-prefix: /ai
    target-prefix: /api
